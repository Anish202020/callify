import React from 'react'
import { FocusCards } from "@/components/ui/focus-cards";
import { cards } from '@/constants/teams';
const page = () => {
     
  return <FocusCards cards={cards} />;
  
}

export default page
